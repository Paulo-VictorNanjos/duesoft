<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LMS DUESOFT - Authentication</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #138acf;
            --primary-dark: #4338ca;
            --secondary: #0508b1;
            --bg-main: #0f172a;
            --bg-card: #1e293b;
            --text-primary: #f8fafc;
            --text-secondary: #cbd5e1;
            --border-color: #334155;
        }

        body {
            background: linear-gradient(135deg, var(--bg-main), var(--primary-dark));
            min-height: 100vh;
            display: flex;
            align-items: center;
            color: var(--text-primary);
        }

        .auth-card {
            background: var(--bg-card);
            border-radius: 1rem;
            box-shadow: 0 25px 50px rgba(0,0,0,0.3);
            padding: 2rem;
            animation: slideUp 0.5s ease-out;
            border: none;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .form-control {
            background-color: var(--bg-main);
            border-color: var(--border-color);
            color: var(--text-primary);
        }

        .form-control:focus {
            background-color: var(--bg-main);
            border-color: var(--primary);
            box-shadow: 0 0 0 0.25rem linear-gradient(90deg, rgba(0,35,36,1) 0%, rgba(9,117,121,1) 44%, rgba(0,212,255,1) 100%);
            color: var(--text-primary);
        }

        .btn-auth {
            background: linear-gradient(90deg, rgba(9,117,121,1) 0%, rgba(10,137,171,1) 22%, rgba(0,212,255,1) 100%);
            border: none;
            color: white;
            padding: 0.75rem;
            border-radius: 0.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-auth:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px linear-gradient(90deg, rgba(0,35,36,1) 0%, rgba(9,117,121,1) 44%, rgba(0,212,255,1) 100%);
        }

        .alert {
            border: none;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .form-label {
            color: var(--text-secondary);
        }

        a {
            color: var(--primary);
            text-decoration: none;
        }

        a:hover {
            color: var(--secondary);
        }
    </style>
</head>
<body>
    <%- body %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>